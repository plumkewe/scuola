#include <iostream>

using namespace std;

//  bho, non ho voglia di sforzare il cervello a quest ora.


int main() {
	setlocale(LC_ALL, "italian");
	
	
	
	
	return 0;
}

//  Un elementi di misura fornisce un dato ogni minuto nell’arco di un’ora. Per ovviare a possibili errori si
//  vogliono elaborare i valori rilevati sostituendoli con una media a tre punti: ogni elemento viene sostituito
//  dalla media di se stesso, dell’elemento che lo precede e dell’elemento che lo segue. Per i due elementi
//  estremi viene considerato due volte il valore dell’elemento stesso e il successivo o il precedente nel caso si
//  tratti rispettivamente del primo o dell’ultimo elemento. Realizzare un programma C++ che implementi
//  l’elaborazione descritta acquisendo i dati da tastiera.